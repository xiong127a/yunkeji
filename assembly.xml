<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <id>dist</id>
    <formats>
        <format>tar.gz</format>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>yunkeji</baseDirectory>

    <fileSets>
        <!-- 添加启动和停止脚本 -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>start-prebuilt.sh</include>
                <include>stop.sh</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- 添加配置文件 -->
        <fileSet>
            <directory>${project.basedir}/yunkeji-boot/src/main/resources</directory>
            <outputDirectory>/config</outputDirectory>
            <includes>
                <include>application.yml</include>
                <include>application-*.yml</include>
                <include>log4j2.xml</include>
            </includes>
        </fileSet>

        <!-- 添加可执行jar包 -->
        <fileSet>
            <directory>${project.basedir}/yunkeji-boot/target</directory>
            <outputDirectory>/lib</outputDirectory>
            <includes>
                <include>yunkeji-boot-*-exec.jar</include>
            </includes>
        </fileSet>

        <!-- 移除前端构建产物，前后端分离部署时不需要 -->

        <!-- 创建日志目录 -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>/logs</outputDirectory>
            <excludes>
                <exclude>*/**</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>