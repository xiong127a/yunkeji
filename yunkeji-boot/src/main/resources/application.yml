server:
  port: 8080
  servlet:
    context-path: /yunkeji

spring:
  datasource:
    url: jdbc:mysql://192.168.102.20:3306/yunkeji?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  
  flyway:
    enabled: false
    locations: classpath:db/migration
    
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
    
logging:
  config: classpath:log4j2.xml

mybatis-flex:
  configuration:
    map-underscore-to-camel-case: true
    # MySQL-specific settings
    database-id: mysql
    
file:
  upload:
    path: ./uploads/
    real-estate:
      path: ./uploads/real-estate/
    
real:
  estate:
    api:
      url: http://8.137.70.50:81
    access:
      key: accessKey
    secret:
      key: secretKey
      
jwt:
  secret: mySecretKey
  expiration: 86400